# gestures{} block reworked in Hyprland 0.55; workspace_swipe was disabled here
# anyway. Workspace swipe is off by default, so nothing needed.
# To re-enable swipe on 0.55, use the new `gesture = ...` syntax (see wiki).
