# cava config used by the waybar audio-visualizer module
[general]
bars = 20
framerate = 60

[input]
method = pulse
source = auto

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 7
