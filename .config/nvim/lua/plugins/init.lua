-- Every .lua file in this directory is loaded automatically by lazy.nvim as a
-- plugin spec, via the `{ import = "plugins" }` line in config/lazy.lua. Add
-- plugins by dropping a new file in here, not by editing this one - this file
-- exists so the import has a module to resolve even when the directory is
-- otherwise empty.
return {}
