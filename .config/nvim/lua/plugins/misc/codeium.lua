-- Codeium AI autocomplete removed (was unauthenticated and nagged on every
-- startup). Returning an empty spec disables the plugin. To bring it back,
-- restore:  return { "Exafunction/codeium.vim" }
return {}
